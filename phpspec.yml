extensions:
    - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
  format:
    - clover
  output:
    clover: coverage.xml

suites:
    core:
        spec_prefix: phpspec
        namespace: Rawkode\Eidetic
        psr4_prefix: Rawkode\Eidetic
        src_path: src
        spec_path: test
